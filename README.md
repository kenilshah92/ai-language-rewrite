# AI Language Rewrite 0.3.0

Private Render release. source.zip contains the complete tested application source, writing prompt, tests, and deployment instructions. The Docker build unpacks it and uses the existing engine. No API keys or customer documents are included.

Regenerate from the local project with `python scripts/package_render.py`, then replace these release files.
